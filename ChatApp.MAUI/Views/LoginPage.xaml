<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ChatApp.MAUI.Views.LoginPage"
             Title="Login">
    <VerticalStackLayout Spacing="16" Padding="24" VerticalOptions="Center">
        <Label Text="Enter Username" FontAttributes="Bold" FontSize="18" />
        <Entry Text="{Binding UserName}" Placeholder="Username" />
        <Button Text="Connect" Command="{Binding ConnectCommand}" IsEnabled="{Binding IsConnecting, Converter={StaticResource InverseBoolConverter}}" />
        <ActivityIndicator IsRunning="{Binding IsConnecting}" IsVisible="{Binding IsConnecting}" />
        <Label TextColor="Red" Text="{Binding ErrorMessage}" />
    </VerticalStackLayout>
</ContentPage>